{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "nexus-scheduler.serviceAccountName" . }}
  labels:
    {{- include "nexus-scheduler.labels" . | nindent 4 }}
{{- end }}
