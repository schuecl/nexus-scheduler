{
  "name": "nexus-scheduler",
  "private": true,
  "version": "0.1.7",
  "description": "Web application for scheduling agentic AI tasks against LibreChat's Agents API.",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/shared",
    "packages/pdf",
    "packages/pdf-service",
    "packages/api",
    "packages/worker",
    "packages/frontend"
  ],
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "scripts": {
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=packages/pdf && npm run build --workspace=packages/pdf-service && npm run build --workspace=packages/api && npm run build --workspace=packages/worker && npm run build --workspace=packages/frontend",
    "lint": "eslint .",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "prisma:generate": "npm run prisma:generate --workspace=packages/shared",
    "prisma:migrate": "npm run prisma:migrate --workspace=packages/shared",
    "release": "node scripts/release.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "eslint": "^10.7.0",
    "eslint-plugin-security": "^3.0.1",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.64.0"
  }
}
