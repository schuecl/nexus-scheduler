import { Typography } from "@mui/material";

export function PromptLibraryPage() {
  return (
    <>
      <Typography variant="h4" gutterBottom>
        Prompt Library
      </Typography>
      <Typography color="text.secondary">
        Searchable, tag-filterable Projects and saved prompts, with favorites and version history
        (REQUIREMENTS §2.3) land here.
      </Typography>
    </>
  );
}
